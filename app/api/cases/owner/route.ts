import { NextResponse } from "next/server";

// Minimal placeholder route to satisfy Next.js type generation.
// If you intend to implement owner-specific endpoints, replace this
// with the full implementation. For now return an empty JSON object.
export async function GET() {
  return NextResponse.json({});
}
