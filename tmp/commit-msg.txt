feat(live): intent auto-advance, end-of-session transcript, EN transcript repair, layout fix

- stage-intent detection: explicit transition phrases ("let's do the
  physical examination") advance the stage automatically, bring the
  incoming persona into focus and trigger their opening line
- completion dialog: collapsible full session transcript via ChatMessage
- non-English input transcriptions are translated to English server-side
  (new /api/live/translate, gpt-4o-mini) and rewritten in place; the model
  still hears the original audio so understanding is unaffected
- layout: min-h-0 chain + flex-shrink-0 on header/controls so the bottom
  controls are never clipped on short windows
