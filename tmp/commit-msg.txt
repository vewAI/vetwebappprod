feat(live): atomic session claim, pagination, intro overlay, live captions and interrupt (F4.2, F4.3, F5.1, F5.2)

F4.2 atomic session:
- db/add_claim_live_attempt.sql: SECURITY DEFINER RPC serializes the
  select-then-insert with FOR UPDATE so concurrent tabs resume the same
  attempt instead of creating duplicates (requires manual SQL run)
- POST /api/live/session tries the RPC first, falls back to legacy flow

F4.3 pagination:
- GET /api/cases capped at 200 (newest first)
- resume and attempt query message reads capped at 1000
- fixed stray backticks in attemptQueryService select strings

F5.1 intro overlay:
- pre-session briefing (case, stages, tips) with Start/Resume button; the
  session no longer auto-connects on mount, which also provides the user
  gesture required by getUserMedia/AudioContext on iOS/Safari
- session timer starts only after start

F5.2 live captions + interrupt:
- interim input transcription exposed as pendingInput and rendered as a
  live caption bar above the controls
- Hand button appears while the persona is speaking to barge-in (interrupt
  + drop queued audio)
